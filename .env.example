# Token du bot Discord (depuis le portail développeur Discord)
DISCORD_TOKEN=ton_token_ici

# Connexion MySQL (Pterodactyl)
# Depuis un container Docker, utilisez l'IP du gateway Docker :
#   - 172.18.0.1 (réseau pterodactyl0, par défaut)
#   - ou l'IP publique du serveur (ex: 37.27.126.113)
# NE PAS utiliser "localhost" ou "127.0.0.1" (pointe vers le container, pas l'hôte)
DB_HOST=172.18.0.1
DB_PORT=3306
DB_USER=u9_9OOfLB45uS
DB_PASSWORD=ton_mot_de_passe_ici
DB_NAME=s9_Sync_discord

# Nom du canal textuel pour les logs d'audit
LOG_CHANNEL_NAME=logs-sync
